extends layout

block content
  h1= title
  a(href='/new')
    button(type='button')= 'Add New Message'
  ul 
    each message in messages 
      li 
        p= message.text 
        p= message.user 
        p= message.added
